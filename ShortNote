npm install -g degit

create project - degit sveltejs/template myproject

npm install

to run project - npm run dev

keywords ---
    on:click
    bind:value
    
    {#each people as person} //people is a list
    {:else}
	{/each}

    {#if num > 20}
	    <p>Greater than 20</p>
    {:else if num>5}
        <p>Greater than 5</p>
    {:else}
        <p>Not greater than 5</p>
    {/if}